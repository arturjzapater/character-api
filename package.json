{
  "name": "silmarillion-api",
  "version": "1.0.0",
  "description": "A REST API to serve information about characters in the Silmarillion.",
  "main": "index.js",
  "scripts": {
    "start": ". ./.env node dist/server.js",
    "test": "mocha --reporter nyan --require @babel/register tests/**/*.test.js",
    "test:watch": "mocha --reporter min --require @babel/register --watch tests/**/*.test.js",
    "dev": ". ./.env && nodemon --exec babel-node src/server.js",
    "build": "babel src --out-dir dist"
  },
  "author": "SavagePixie",
  "license": "MIT",
  "dependencies": {
    "@babel/preset-env": "^7.10.4",
    "express": "^4.17.1",
    "fluture": "^12.3.1",
    "ramda": "^0.27.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.3",
    "@babel/core": "^7.10.3",
    "@babel/node": "^7.10.3",
    "@babel/plugin-proposal-pipeline-operator": "^7.10.1",
    "@babel/register": "^7.10.4",
    "babel-eslint": "^10.1.0",
    "babel-preset-minify": "^0.5.1",
    "eslint": "^7.3.1",
    "mocha": "^8.0.1",
    "nodemon": "^2.0.4"
  }
}
